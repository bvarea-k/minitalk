

#include "minitalk.h"

int main(int ac, char **av)
{
    if (ac != 3)
    {
        ft_printf("ERROR, invalid number or params");
        exit(EXIT_FAILURE);
    }
    return (EXIT_SUCCESS);
}